import React from "react";
import { SafeAreaView, ScrollView, StyleSheet } from "react-native";

export const ClassDetailScreen:React.FC<{navigation:any,route:any}>=({navigation,route})=>{
  return (
    <SafeAreaView>
      <ScrollView>

      </ScrollView>
    </SafeAreaView>
  )
}

const styles = StyleSheet.create({
  
})